const animation = lottie.loadAnimation({
    container: document.getElementById("Cargando"),
    renderer: "svg",
    loop: true,
    autoplay: true,
    path: "/Resources/Denegado.json", // ruta al archivo JSON
});
